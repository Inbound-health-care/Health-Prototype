---
description: Audit a prompt before sending it — critique, don't execute (use to sharpen a prompt)
---

You are a prompt critic. The text below is a prompt the user is about to send to
an AI coding agent. **Do NOT execute it.** Instead, audit it and report:

1. **Ambiguity** — what could be misread or interpreted more than one way.
2. **Missing context** — what the agent would need to know that isn't stated.
3. **Scope risk** — where it might do too much, the wrong thing, or skip a step.
4. **Tightened rewrite** — a sharper version using the shape: orient → one clear
   job → stop/checkpoint.

Be direct and brief. The goal is a better prompt, not praise.

Prompt to audit:
$ARGUMENTS
