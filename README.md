# Health-Prototype
The one function — detect_recurrence, with a suggested signature and the generic record shapeExact-match for v0 — fuzzy/synonym matching explicitly deferred to v1 so you prove the core first
